# Log linter

Instruction

- `go test ./analyzer` to run unit tests
- `golangci-lint custom` to build a custom golangci-lint executable with the plugin
- `custom-golangci-lint run [file]` to lint the code
-  Use `.golangci.yml` to configure sensitive data patterns.